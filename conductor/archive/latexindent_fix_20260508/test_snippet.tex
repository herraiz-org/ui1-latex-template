\documentclass{article}
\begin{document}

Some text with \textbf{a long argument that
needs to wrap onto a second line}.

Some text with \emph{a long argument that
needs to wrap onto a second line}.

Some text with \textit{a long argument that
needs to wrap onto a second line}.

Some text with \textsf{a long argument that
needs to wrap onto a second line}.

Some text with \texttt{a long argument that
needs to wrap onto a second line}.

Some text with \underline{a long argument that
needs to wrap onto a second line}.

\begin{itemize}
  \item First item
  \item Second item
\end{itemize}

\section{A Section}

Some paragraph text.

\end{document}
